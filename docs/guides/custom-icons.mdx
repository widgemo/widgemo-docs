---
sidebar_position: 3
---

import { defaultRenderWidgemoIcon } from '@widgemo/widgemo-core';

# Custom Icons Guide

widgemo-core uses a named icon registry. Any built-in icon name can be overridden, and new names can be added.

## Register a Custom Icon

```tsx
import { widgemoRegistry } from '@widgemo/widgemo-core';
import { CalendarIcon } from '@heroicons/react/24/outline';

widgemoRegistry.registerWidgemoIcon({
	name: 'calendar',
	component: ({ size = 16, className, color = 'currentColor' }) => (
		<CalendarIcon
			width={size}
			height={size}
			className={className}
			style={{ color }}
		/>
	),
});
```

The icon component receives `{ size?, className?, color? }`. Returning `null` falls back to the built-in SVG.

## Built-in Icon Names

The list below reflects all currently available built-in icon names in core.

export const builtInIconGroups = [
	{ icon: 'add', names: ['add', 'plus'] },
	{ icon: 'edit', names: ['edit', 'pencil'] },
	{ icon: 'delete', names: ['delete', 'trash'] },
	{ icon: 'view', names: ['view', 'eye'] },
	{ icon: 'view-slash', names: ['view-slash', 'eye-slash'] },
	{ icon: 'refresh', names: ['refresh', 'reload'] },
	{ icon: 'settings', names: ['settings', 'config', 'cog'] },
	{ icon: 'close', names: ['close', 'x', 'cross'] },
	{ icon: 'chevron-down', names: ['chevron-down'] },
	{ icon: 'chevron-up', names: ['chevron-up'] },
	{ icon: 'chevron-left', names: ['chevron-left'] },
	{ icon: 'chevron-right', names: ['chevron-right'] },
	{ icon: 'arrow-up', names: ['arrow-up'] },
	{ icon: 'arrow-down', names: ['arrow-down'] },
	{ icon: 'arrow-left', names: ['arrow-left'] },
	{ icon: 'arrow-right', names: ['arrow-right'] },
	{ icon: 'arrow-down-left', names: ['arrow-down-left'] },
	{ icon: 'arrow-down-right', names: ['arrow-down-right'] },
	{ icon: 'arrow-up-left', names: ['arrow-up-left'] },
	{ icon: 'arrow-up-right', names: ['arrow-up-right'] },
	{ icon: 'search', names: ['search'] },
	{ icon: 'speedometer', names: ['speedometer'] },
	{ icon: 'filter', names: ['filter'] },
	{ icon: 'filter-off', names: ['filter-off'] },
	{ icon: 'sort', names: ['sort'] },
	{ icon: 'chart-bar', names: ['chart-bar'] },
	{ icon: 'chart-line', names: ['chart-line'] },
	{ icon: 'chart-pie', names: ['chart-pie'] },
	{ icon: 'table', names: ['table'] },
	{ icon: 'grid', names: ['grid'] },
	{ icon: 'board', names: ['board'] },
	{ icon: 'bug', names: ['bug', 'defect'] },
	{ icon: 'document', names: ['document'] },
	{ icon: 'credit-card', names: ['credit-card'] },
	{ icon: 'map', names: ['map'] },
	{ icon: 'calendar', names: ['calendar'] },
	{ icon: 'goal', names: ['goal', 'target'] },
	{ icon: 'warning', names: ['warning'] },
	{ icon: 'bell', names: ['bell', 'alert', 'notification'] },
	{ icon: 'sun', names: ['sun', 'light-mode'] },
	{ icon: 'moon', names: ['moon', 'dark-mode'] },
	{ icon: 'info', names: ['info'] },
	{ icon: 'shield', names: ['shield'] },
	{ icon: 'building', names: ['building'] },
	{ icon: 'server', names: ['server'] },
	{ icon: 'database', names: ['database'] },
	{ icon: 'users', names: ['users'] },
	{ icon: 'code', names: ['code', 'html'] },
	{ icon: 'copy', names: ['copy'] },
	{ icon: 'export', names: ['export', 'download'] },
	{ icon: 'share', names: ['share'] },
	{ icon: 'ellipsis-vertical', names: ['ellipsis-vertical'] },
	{ icon: 'ellipsis-horizontal', names: ['ellipsis-horizontal'] },
	{ icon: 'star', names: ['star'] },
	{ icon: 'heart', names: ['heart'] },
	{ icon: 'currency-dollar', names: ['currency-dollar'] },
	{ icon: 'external-link', names: ['external-link', 'new-tab'] },
	{ icon: 'group', names: ['group', 'grouping'] },
	{ icon: 'group-available', names: ['group-available'] },
	{ icon: 'square', names: ['square'] },
	{ icon: 'check-square', names: ['check-square'] },
	{ icon: 'x-square', names: ['x-square'] },
	{ icon: 'align-center', names: ['align-center'] },
	{ icon: 'dollar-sign', names: ['dollar-sign', 'dollar'] },
	{ icon: 'cash', names: ['cash'] },
	{ icon: 'asset', names: ['asset', 'mortgage'] },
	{ icon: 'budget', names: ['budget'] },
	{ icon: 'loan', names: ['loan'] },
	{ icon: 'real-estate', names: ['real-estate', 'house'] },
	{ icon: 'car', names: ['car'] },
	{ icon: 'stock', names: ['stock'] },
	{ icon: 'crypto', names: ['crypto'] },
	{ icon: 'transfer', names: ['transfer'] },
	{ icon: 'shopping-cart', names: ['shopping-cart'] },
	{ icon: 'puzzle-piece', names: ['puzzle-piece'] },
	{ icon: 'circle', names: ['circle'] },
	{ icon: 'check-circle', names: ['check-circle'] },
	{ icon: 'x-circle', names: ['x-circle'] },
	{ icon: 'clock', names: ['clock'] },
	{ icon: 'pause', names: ['pause'] },
];

<div
	style={{
		display: 'grid',
		gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))',
		gap: '0.75rem',
		marginTop: '1rem',
	}}
>
	{builtInIconGroups.map((group) => (
		<div
			key={group.icon}
			style={{
				border: '1px solid var(--ifm-color-emphasis-300)',
				borderRadius: '10px',
				padding: '0.6rem 0.75rem',
				display: 'flex',
				alignItems: 'center',
				gap: '0.6rem',
				background: 'var(--ifm-background-surface-color)',
			}}
		>
			<span
				aria-hidden
				style={{
					display: 'inline-flex',
					alignItems: 'center',
					justifyContent: 'center',
					width: '22px',
					height: '22px',
					color: 'var(--ifm-color-primary)',
					flexShrink: 0,
				}}
			>
				{defaultRenderWidgemoIcon({ name: group.icon, size: 18 })}
			</span>
			<code style={{ fontSize: '0.76rem', lineHeight: 1.2 }}>{group.names.join(', ')}</code>
		</div>
	))}
</div>

## Override a Built-in Icon

Register under the same name to replace it globally:

```tsx
widgemoRegistry.registerWidgemoIcon({
	name: 'edit',
	component: ({ size = 16, color = 'currentColor' }) => (
		<MyCustomEditIcon width={size} style={{ color }} />
	),
});
```

## Collision Behavior

Collision semantics are defined in [Extension API: Registration Collisions](/core/reference/extension-api#registration-collisions).

Use namespaced names (for example, `acme.edit`) to reduce accidental collisions.

## Using Icons in Config

Anywhere `icon` accepts a string, provide the registered name:

```tsx
actions: [
	{ id: 'schedule', label: 'Schedule', icon: 'calendar', placement: 'pinned' },
]
```

```tsx
zones: {
	header: {
		icon: { name: 'calendar', size: 24, color: '#4f46e5' },
	},
}
```
