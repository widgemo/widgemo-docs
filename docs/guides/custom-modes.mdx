---
sidebar_position: 2
---

import { WidgemoShowcase } from '../../src/components/WidgemoShowcase';

# Custom Modes Guide

Custom modes let you build an entirely new layout and rendering strategy for your dataset, then ship it as a named value in `content.mode`.

<WidgemoShowcase
	title="Why Custom Modes Exist"
	description="Use built-in modes first, then move to custom mode registration when your layout strategy cannot be represented as table, grid, board, carousel, or chart."
	data={[
		{ phase: 'Built-in mode fit', recommendation: 'Use config only', status: 'preferred' },
		{ phase: 'Domain-specific layout', recommendation: 'Register custom mode', status: 'extension' },
	]}
	config={{
		zones: {
			content: {
				mode: 'table',
				item: {
					fields: [
						{ key: 'phase', label: 'Phase' },
						{ key: 'recommendation', label: 'Recommendation' },
						{ key: 'status', label: 'Status', renderAs: 'badge' },
					],
					layout: { type: 'auto' },
				},
			},
		},
	}}
/>

## 1. Build the Mode Component

A mode component receives a standard props contract:

```tsx
// src/modes/TimelineMode.tsx
import React from 'react';
import type { Entity } from '@widgemo/widgemo-core';

interface TimelineModeProps {
	data: Entity[];
	config?: Record<string, unknown>;
	actions?: unknown[];
	onInteractionEvent?: (ctx: unknown) => void;
}

export function TimelineMode({ data, config = {} }: TimelineModeProps) {
	const { dateField = 'date', sortOrder = 'desc' } = config as {
		dateField?: string;
		sortOrder?: 'asc' | 'desc';
	};

	const sorted = [...data].sort((a, b) => {
		const d =
			new Date(String(a[dateField])).getTime() -
			new Date(String(b[dateField])).getTime();
		return sortOrder === 'asc' ? d : -d;
	});

	return (
		<ol
			style={{
				listStyle: 'none',
				padding: 0,
				margin: 0,
				display: 'flex',
				flexDirection: 'column',
				gap: '0.75rem',
			}}
		>
			{sorted.map((entity, i) => (
				<li
					key={i}
					style={{ display: 'flex', gap: '0.75rem', alignItems: 'flex-start' }}
				>
					<time style={{ minWidth: 90, color: '#6b7280', fontSize: '0.8rem' }}>
						{String(entity[dateField])}
					</time>
					<span style={{ fontWeight: 500 }}>
						{String(entity.title ?? entity.name ?? '—')}
					</span>
				</li>
			))}
		</ol>
	);
}
```

## 2. Register at App Boot

```tsx
import { widgemoRegistry } from '@widgemo/widgemo-core';
import { TimelineMode } from './modes/TimelineMode';

widgemoRegistry.registerWidgemoMode({
	name: 'timeline',
	component: TimelineMode,
	defaultConfig: {
		dateField: 'date',
		sortOrder: 'desc',
		orientation: 'vertical',
		showLines: true,
		color: '#007bff',
	},
});
```

## 3. Use in Config

```tsx
const config = {
	zones: {
		header: { title: 'Project Timeline' },
		content: {
			mode: 'timeline',
			modeConfig: {
				timeline: {
					dateField: 'created_at',
					sortOrder: 'asc',
				},
			},
			item: { fields: […], layout: { type: 'auto' } },
		},
	},
};
```

## Mode Props Contract

| Prop | Type | Description |
| --- | --- | --- |
| `data` | `Entity[]` | Filtered, sorted, paginated dataset |
| `config` | `Record<string, unknown>` | Mode-specific config from `modeConfig.<name>` |
| `actions` | `ActionConfig[]` | Forwarded item actions |
| `gestures` | `GestureConfig[]` | Forwarded gestures |
| `onInteractionEvent` | `(ctx) => void` | Event emitter for interaction context |

## Interactions in Custom Modes

Built-in gesture types available through config:

- `item-click`
- `item-drag-start`
- `item-drop`

Gesture handler flow:

- If the matching gesture defines `onTrigger`, Widgemo calls `onTrigger`.
- If `onTrigger` is omitted, Widgemo falls back to `interactions.onEvent`.

For gesture configuration details, see [Widgemo Config: Gestures](/core/reference/widgemo-config#gestures).

```tsx
const config = {
	interactions: {
		onEvent: (ctx) => {
			console.log('fallback interaction', ctx.kind, ctx.entity?.id);
		},
	},
	zones: {
		content: {
			mode: 'timeline',
			gestures: [
				{
					type: 'item-click',
					onTrigger: (ctx) => {
						openEntity(ctx.entity?.id);
					},
				},
				{ type: 'item-drag-start' },
				{ type: 'item-drop' },
			],
			item: { fields: [{ key: 'name' }], layout: { type: 'auto' } },
		},
	},
};
```

### Mode-Local Interactions

You can implement additional mode-specific UI interactions directly in your custom mode component (for example hover effects, drag-over highlights, and scroll-driven behaviors).

Use built-in gestures for standardized interaction handling. Use mode-local event handlers for mode-specific interactions.

```tsx
export function TimelineMode({ data = [], onInteractionEvent }: TimelineModeProps) {
	return (
		<ol>
			{data.map((entity, index) => (
				<li
					key={String(entity.id ?? index)}
					onMouseEnter={() => highlightRow(entity.id)}
					onWheel={(event) => syncTimelineScroll(event.deltaY)}
					onDoubleClick={() => onInteractionEvent?.({
						kind: 'item-click',
						zone: 'content',
						entity,
						data,
						interactionId: 'timeline-double-click',
						interactionLabel: 'Timeline Double Click',
					})}
				>
					{String(entity.name ?? entity.title ?? index)}
				</li>
			))}
		</ol>
	);
}
```

For lifecycle interception guidance, see [Extension API: When to Use Which](/core/reference/extension-api#when-to-use-which).

## Collision Behavior

Collision semantics are defined in [Extension API: Registration Collisions](/core/reference/extension-api#registration-collisions).

Use namespaced names (for example, `acme.timeline`) to reduce accidental collisions.

## Related

- [Extension API](/core/reference/extension-api)
- [Extension Architecture](/core/concepts/extension-architecture)
- [Custom renderAs](/core/extensions/renderas-customization)
- [Docs Map](/core/getting-started/docs-map)

## Continue Reading

- Previous: [Custom Field Types](/core/extensions/custom-field-types)
- Next: [Custom Icons](/core/guides/custom-icons)
