---
sidebar_position: 5
---

import { WidgemoShowcase } from '../../../src/components/WidgemoShowcase';

export const barChartData = [
  { month: 'Jan', revenue: 120, visits: 180 },
  { month: 'Feb', revenue: 150, visits: 210 },
  { month: 'Mar', revenue: 132, visits: 190 },
  { month: 'Apr', revenue: 174, visits: 230 },
];

export const lineChartData = [
  { month: 'Jan', revenue: 90, visits: 160 },
  { month: 'Feb', revenue: 120, visits: 190 },
  { month: 'Mar', revenue: 165, visits: 205 },
  { month: 'Apr', revenue: 180, visits: 220 },
];

export const areaChartData = [
  { month: 'Jan', revenue: 95, visits: 140 },
  { month: 'Feb', revenue: 130, visits: 170 },
  { month: 'Mar', revenue: 160, visits: 200 },
  { month: 'Apr', revenue: 175, visits: 225 },
];

export const pieChartData = [
  { segment: 'Product', value: 42 },
  { segment: 'Support', value: 21 },
  { segment: 'Services', value: 17 },
  { segment: 'Other', value: 20 },
];

export const donutChartData = [
  { segment: 'Desktop', value: 55 },
  { segment: 'Tablet', value: 18 },
  { segment: 'Mobile', value: 27 },
];

export const barChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'month',
          height: 320,
          showLegend: true,
          showGrid: true,
          series: [
            { key: 'revenue', label: 'Revenue', type: 'bar' },
            { key: 'visits', label: 'Visits', type: 'bar' },
          ],
        },
      },
    },
  },
};

export const lineChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'month',
          height: 320,
          showLegend: true,
          showGrid: true,
          showDots: true,
          series: [
            { key: 'revenue', label: 'Revenue', type: 'line' },
            { key: 'visits', label: 'Visits', type: 'line' },
          ],
        },
      },
    },
  },
};

export const areaChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'month',
          height: 320,
          showLegend: true,
          showGrid: true,
          showDots: false,
          areaGradient: true,
          series: [
            { key: 'revenue', label: 'Revenue', type: 'area' },
            { key: 'visits', label: 'Visits', type: 'area' },
          ],
        },
      },
    },
  },
};

export const pieChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'segment',
          height: 320,
          showLegend: true,
          showGrid: false,
          series: [
            { key: 'value', label: 'Share', type: 'pie' },
          ],
        },
      },
    },
  },
};

export const donutChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'segment',
          height: 320,
          showLegend: true,
          showGrid: false,
          donutInnerRadiusRatio: 0.68,
          series: [
            { key: 'value', label: 'Share', type: 'donut' },
          ],
        },
      },
    },
  },
};

export const horizontalBarChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'month',
          height: 280,
          orientation: 'horizontal',
          showLegend: true,
          series: [
            { key: 'revenue', label: 'Revenue', type: 'bar' },
          ],
        },
      },
    },
  },
};

export const perSeriesChartConfig = {
  zones: {
    content: {
      mode: 'chart',
      modeConfig: {
        chart: {
          xAxis: 'month',
          height: 300,
          showDots: false,
          lineThickness: 1.5,
          series: [
            { key: 'revenue', label: 'Revenue', type: 'line', showDots: true, color: '#6366f1', lineThickness: 3 },
            { key: 'visits', label: 'Visits', type: 'line' },
          ],
        },
      },
    },
  },
};

# Chart Mode

`modeConfig.chart` owns the full content area as a chart. Use it when the visualization itself is the primary surface, not a small field renderer. For inline composition bars, see [compositionBar vs Chart Mode](/core/components/renderers/composition-bar-guide#when-to-use-compositionbar-vs-chart-mode).

Five series types are supported: `bar`, `line`, `area`, `pie`, and `donut`. Several options (`color`, `lineThickness`, `showDots`, `areaGradient`, `donutInnerRadiusRatio`) can be set at the top-level chart config as a default, or overridden per series.

## Shared Behavior

Search, static filtering, pagination, actions, and gestures work the same as other modes — see the reference pages:

- [Content Config: Actions and Interactions](/core/reference/content-config#actions)
- [Content Config: Pagination and Search](/core/reference/content-config#pagination-and-search)
- [Content Config: Static Filtering](/core/reference/content-config#static-filtering-and-sorting)
- [Content Config: Shape](/core/reference/content-config#shape) for responsive, loading, and error fields
- [Field Config](/core/reference/field-config) for field-level rendering rules outside chart-only concerns

## Core Examples

### Bar Chart

Use bar charts for direct comparisons across categories.

<WidgemoShowcase
  description="A vertical bar chart with two series, grid lines, and a legend."
  data={barChartData}
  config={barChartConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  chart: {
    xAxis: 'month',
    height: 320,
    series: [
      { key: 'revenue', label: 'Revenue', type: 'bar' },
      { key: 'visits', label: 'Visits', type: 'bar' },
    ],
  },
}
```

### Line Chart

Use line charts when the main story is the change over time.

<WidgemoShowcase
  description="A multi-series line chart with data points enabled."
  data={lineChartData}
  config={lineChartConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  chart: {
    xAxis: 'month',
    height: 320,
    showDots: true,
    series: [
      { key: 'revenue', label: 'Revenue', type: 'line' },
      { key: 'visits', label: 'Visits', type: 'line' },
    ],
  },
}
```

### Area Chart

Use area charts when you want the same trend view with more visual weight.

<WidgemoShowcase
  description="A multi-series area chart with gradient fills."
  data={areaChartData}
  config={areaChartConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  chart: {
    xAxis: 'month',
    height: 320,
    areaGradient: true,
    series: [
      { key: 'revenue', label: 'Revenue', type: 'area' },
      { key: 'visits', label: 'Visits', type: 'area' },
    ],
  },
}
```

### Pie Chart

Use pie charts when a single category breakdown is easier to read than a cartesian chart.

<WidgemoShowcase
  description="A single polar series with category labels and a visible legend."
  data={pieChartData}
  config={pieChartConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  chart: {
    xAxis: 'segment',
    height: 320,
    showGrid: false,
    series: [
      { key: 'value', label: 'Share', type: 'pie' },
    ],
  },
}
```

### Donut Chart

Use donut charts when you want the same category breakdown with more center breathing room.

<WidgemoShowcase
  description="The same polar data rendered as a donut with a custom inner radius."
  data={donutChartData}
  config={donutChartConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  chart: {
    xAxis: 'segment',
    height: 320,
    showGrid: false,
    donutInnerRadiusRatio: 0.68,
    series: [
      { key: 'value', label: 'Share', type: 'donut' },
    ],
  },
}
```

## See Also

- [Chart Mode Config](/core/reference/mode-config/chart) — full `modeConfig.chart` options, caveats, and coverage summary
- [Content Config](/core/reference/content-config)
- [Field Config](/core/reference/field-config)
- [compositionBar vs Chart Mode](/core/components/renderers/composition-bar-guide#when-to-use-compositionbar-vs-chart-mode)