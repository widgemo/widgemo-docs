---
sidebar_position: 1
---

import { WidgemoShowcase } from '../../../src/components/WidgemoShowcase';

export const tableOptionData = [
  { name: 'Aurora Chen', team: 'Design', status: 'active', score: 98, owner: 'Leah', priority: 'P1' },
  { name: 'Mateo Silva', team: 'Platform', status: 'pending', score: 84, owner: 'Samir', priority: 'P2' },
  { name: 'Priya Nair', team: 'Operations', status: 'inactive', score: 72, owner: 'Iris', priority: 'P3' },
  { name: 'Niko Reyes', team: 'Design', status: 'active', score: 91, owner: 'Leah', priority: 'P1' },
];

export const tableModeData = [
  { name: 'Aurora Chen', team: 'Design', status: 'active' },
  { name: 'Mateo Silva', team: 'Platform', status: 'pending' },
  { name: 'Priya Nair', team: 'Operations', status: 'inactive' },
];

export const tableHookData = [
  { name: 'Aurora Chen', team: 'Design', status: 'active', score: 98 },
  { name: 'Mateo Silva', team: 'Platform', status: 'pending', score: 84 },
  { name: 'Priya Nair', team: 'Operations', status: 'inactive', score: 72 },
  { name: 'Niko Reyes', team: 'Design', status: 'active', score: 91 },
];

export const tableTraditionalConfig = {
  zones: {
    content: {
      mode: 'table',
      modeConfig: {
        table: {
          type: 'traditional',
          alternatingRows: true,
          hover: true,
          showHeader: true,
          rowSeparator: true,
        },
      },
      item: {
        fields: [
          { key: 'name', label: 'Name' },
          { key: 'team', label: 'Team' },
          { key: 'status', label: 'Status', renderAs: 'badge' },
        ],
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableRichCellsConfig = {
  zones: {
    content: {
      mode: 'table',
      modeConfig: {
        table: {
          type: 'rich-cells',
          columns: 2,
          showHeader: false,
          hover: true,
        },
      },
      item: {
        fields: [
          { key: 'name', label: 'Name' },
          { key: 'team', label: 'Team' },
          { key: 'status', label: 'Status', renderAs: 'badge' },
          { key: 'owner', label: 'Owner' },
          { key: 'priority', label: 'Priority' },
        ],
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableTogglesConfig = {
  zones: {
    content: {
      mode: 'table',
      modeConfig: {
        table: {
          type: 'traditional',
          hover: false,
          showHeader: false,
          alternatingRows: false,
          rowSeparator: false,
        },
      },
      item: {
        fields: [
          { key: 'name', label: 'Name' },
          { key: 'team', label: 'Team' },
          { key: 'status', label: 'Status', renderAs: 'badge' },
        ],
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableConditionalConfig = {
  zones: {
    content: {
      mode: 'table',
      modeConfig: {
        table: {
          type: 'traditional',
          alternatingRows: false,
          conditionalBackgroundColor: (entity) => {
            if (entity.status === 'active') {
              return { backgroundColor: '#16a34a', color: '#f0fdf4' };
            }
            if (entity.status === 'pending') {
              return { backgroundColor: '#d97706', color: '#fffbeb' };
            }
            return { backgroundColor: '#4f46e5', color: '#eef2ff' };
          },
        },
      },
      item: {
        fields: [
          { key: 'name', label: 'Name' },
          { key: 'team', label: 'Team' },
          { key: 'status', label: 'Status', renderAs: 'badge' },
        ],
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableHooksConfig = {
  zones: {
    content: {
      mode: 'table',
      sorting: [{ fieldKey: 'score', direction: 'desc' }],
      groupings: [
        {
          fieldKey: 'team',
          initiallyCollapsed: false,
          collapsible: true,
          showDropdownControl: true,
          showHeaderControls: true,
        },
      ],
      modeConfig: {
        table: {
          hooks: {
            onSort: (field, direction) => console.log('sort changed', field, direction),
            onGroup: (field) => console.log('group changed', field),
            preRowRender: (entity) => entity,
            onGroupToggle: (groupValue, isExpanded) =>
              console.log('group toggle', groupValue, isExpanded),
          },
        },
      },
      item: {
        fields: [
          { key: 'name', label: 'Name', sortable: true, groupable: true },
          { key: 'team', label: 'Team', sortable: true, groupable: true },
          { key: 'status', label: 'Status', renderAs: 'badge', sortable: true, groupable: true },
          { key: 'score', label: 'Score', sortable: true, align: 'right' },
        ],
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableGroupingControlData = [
  { name: 'Aurora Chen', team: 'Design', status: 'active', score: 98 },
  { name: 'Mateo Silva', team: 'Platform', status: 'pending', score: 84 },
  { name: 'Priya Nair', team: 'Operations', status: 'inactive', score: 72 },
  { name: 'Niko Reyes', team: 'Design', status: 'active', score: 91 },
  { name: 'Elena Park', team: 'Platform', status: 'active', score: 88 },
  { name: 'Jonas Bell', team: 'Operations', status: 'pending', score: 79 },
];

export const groupingControlFields = [
  { key: 'name', label: 'Name', sortable: true },
  { key: 'team', label: 'Team', sortable: true, groupable: true },
  { key: 'status', label: 'Status', renderAs: 'badge', sortable: true, groupable: true },
  { key: 'score', label: 'Score', sortable: true, align: 'right' },
];

export const tableGroupingDropdownOnlyConfig = {
  zones: {
    content: {
      mode: 'table',
      groupings: [
        {
          fieldKey: 'team',
          initiallyCollapsed: false,
          showDropdownControl: true,
          showHeaderControls: false,
        },
      ],
      item: {
        fields: groupingControlFields,
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableGroupingIconsOnlyConfig = {
  zones: {
    content: {
      mode: 'table',
      groupings: [
        {
          fieldKey: 'team',
          initiallyCollapsed: false,
          showHeaderControls: true,
        },
      ],
      item: {
        fields: groupingControlFields,
        layout: { type: 'auto' },
      },
    },
  },
};

export const tableGroupingBothControlsConfig = {
  zones: {
    content: {
      mode: 'table',
      groupings: [
        {
          fieldKey: 'team',
          initiallyCollapsed: false,
          showDropdownControl: true,
          showHeaderControls: true,
        },
      ],
      item: {
        fields: groupingControlFields,
        layout: { type: 'auto' },
      },
    },
  },
};

# Table Mode

Table mode renders data as a structured grid with column headers, sortable columns, and row-level comparisons. It is the best choice when users need to scan many records across multiple fields simultaneously.

The primary structural decision is `type`:
- `traditional` — classic row/column table with optional striping, hover, and separators
- `rich-cells` — multi-column cell blocks per row; better for records with many fields at a glance

## Shared Behavior

Shared content behavior remains canonical in Reference:

- [Content Config: Pagination and Search](/core/reference/content-config#pagination-and-search)
- [Content Config: Static Filtering and Sorting](/core/reference/content-config#static-filtering-and-sorting)
- [Content Config: Actions and Interactions](/core/reference/content-config#actions)
- [Content Config: Shape](/core/reference/content-config#shape) for responsive breakpoints and shared content contract fields

For field-level controls (label, width, alignment, renderAs, groupable, sortable), see [Field Config](/core/reference/field-config).

## Core Examples

### Traditional Table

<WidgemoShowcase
  description="Default table shape with header, hover, alternating rows, and row separators."
  data={tableModeData}
  config={tableTraditionalConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  table: {
    type: 'traditional',
    alternatingRows: true,
    hover: true,
    showHeader: true,
    rowSeparator: true,
  },
}
```

<div style={{ marginBottom: '1rem' }} />

### Rich Cells

<WidgemoShowcase
  description="The same dataset with rich-cells layout — multiple fields per row block, no column header."
  data={tableOptionData}
  config={tableRichCellsConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  table: {
    type: 'rich-cells',
    columns: 2,
    showHeader: false,  // rich-cells has no meaningful column header
    hover: true,
  },
}
```

<div style={{ marginBottom: '1rem' }} />

### Presentation Toggles

<WidgemoShowcase
  description="A minimal table surface with hover, header, striping, and row separators all disabled."
  data={tableModeData}
  config={tableTogglesConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  table: {
    type: 'traditional',
    hover: false,
    showHeader: false,
    alternatingRows: false,
    rowSeparator: false,
  },
}
```

<div style={{ marginBottom: '1rem' }} />

### Conditional Row Styling

<WidgemoShowcase
  description="Each row is colored based on its status value via conditionalBackgroundColor."
  data={tableModeData}
  config={tableConditionalConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  table: {
    type: 'traditional',
    alternatingRows: false,
    conditionalBackgroundColor: (entity) => {
      if (entity.status === 'active') return { backgroundColor: '#16a34a', color: '#f0fdf4' };
      if (entity.status === 'pending') return { backgroundColor: '#d97706', color: '#fffbeb' };
      return { backgroundColor: '#4f46e5', color: '#eef2ff' };
    },
  },
}
```

<div style={{ marginBottom: '1rem' }} />

## Grouping UI Controls

Grouping is configured under `zones.content.groupings`; the canonical contract and cross-mode behavior live in [Content Config: Grouping](/core/reference/content-config#grouping).

This section is intentionally table-specific and focuses on grouping interaction surfaces that apply only in table mode.

| Option | Values | Default | Effect |
|---|---|---|---|
| showDropdownControl | true or false | false | Shows the group-by dropdown control above the table. |
| showHeaderControls | true or false | true | Shows grouping icons in header cells for groupable fields. |

Dropdown visibility still requires at least one field with `groupable: true` so the control has selectable columns.

### Dropdown Only

<WidgemoShowcase
  description="Only the dropdown is shown. Header grouping icons are disabled."
  data={tableGroupingControlData}
  config={tableGroupingDropdownOnlyConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
// In zones.content.groupings:
{ fieldKey: 'team', showDropdownControl: true, showHeaderControls: false }
```

<div style={{ marginBottom: '1rem' }} />

### Header Icons Only

<WidgemoShowcase
  description="Only header grouping icons are shown on groupable columns."
  data={tableGroupingControlData}
  config={tableGroupingIconsOnlyConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
// In zones.content.groupings:
{ fieldKey: 'team', showHeaderControls: true }
```

<div style={{ marginBottom: '1rem' }} />

### Dropdown and Header Icons

<WidgemoShowcase
  description="Both grouping control surfaces are enabled."
  data={tableGroupingControlData}
  config={tableGroupingBothControlsConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
// In zones.content.groupings:
{ fieldKey: 'team', showDropdownControl: true, showHeaderControls: true }
```

<div style={{ marginBottom: '1rem' }} />

### Hook Callbacks

Sort headers and grouping controls trigger the hook callbacks below. Open the browser console to see output.

`preRowRender` receives each entity before it renders and returns the (optionally transformed) entity — useful for normalizing display values or injecting computed fields without modifying the source data.

<WidgemoShowcase
  description="Sort and group interactions trigger table hook callbacks — check the browser console."
  data={tableHookData}
  config={tableHooksConfig}
/>

<div style={{ marginBottom: '1rem' }} />

```tsx
modeConfig: {
  table: {
    hooks: {
      onSort: (field, direction) => console.log('sort changed', field, direction),
      onGroup: (field) => console.log('group changed', field),
      onGroupToggle: (groupValue, isExpanded) => console.log('group toggle', groupValue, isExpanded),
      // preRowRender: (entity) => ({ ...entity, name: entity.name.toUpperCase() }),
    },
  },
}
```

## See Also

- [Table Mode Config](/core/reference/mode-config/table) — full `modeConfig.table` options, caveats, and coverage summary
- [Content Config](/core/reference/content-config)
- [Field Config](/core/reference/field-config)
- [Host CSS and Table Layout](/core/reference/host-css-table-layout)
