import { WidgemoShowcase } from '../../src/components/WidgemoShowcase';

export const modesOverviewData = [
	{ name: 'Aurora Chen', department: 'Engineering', status: 'active', value: 42 },
	{ name: 'Mateo Silva', department: 'Design', status: 'pending', value: 31 },
	{ name: 'Priya Nair', department: 'Operations', status: 'inactive', value: 27 },
	{ name: 'Samir Khan', department: 'Engineering', status: 'active', value: 50 },
];

export const modesOverviewTableConfig = {
	zones: {
		content: {
			mode: 'table',
			modeConfig: {
				table: {
					type: 'traditional',
					alternatingRows: true,
					hover: true,
					showHeader: true,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewGridConfig = {
	zones: {
		content: {
			mode: 'grid',
			modeConfig: {
				grid: {
					minItemWidth: '180px',
					maxColumns: 4,
					gap: '0.75rem',
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewCarouselConfig = {
	zones: {
		content: {
			mode: 'carousel',
			modeConfig: {
				carousel: {
					itemWidth: 220,
					itemHeight: 150,
					gap: 12,
					showArrows: true,
					showIndicators: true,
					infinite: true,
					revolving: true,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewBoardConfig = {
	zones: {
		content: {
			mode: 'board',
			modeConfig: {
				board: {
					columns: {
						field: 'status',
						items: [
							{ id: 'active', label: 'Active', value: 'active' },
							{ id: 'pending', label: 'Pending', value: 'pending' },
							{ id: 'inactive', label: 'Inactive', value: 'inactive' },
						],
					},
					dragEnabled: false,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewBarChartConfig = {
	zones: {
		content: {
			mode: 'chart',
			modeConfig: {
				chart: {
					xAxis: 'name',
					height: 240,
					showLegend: false,
					showGrid: true,
					series: [{ key: 'value', label: 'Value', type: 'bar' }],
				},
			},
		},
	},
};

export const modesOverviewPieChartConfig = {
	zones: {
		content: {
			mode: 'chart',
			modeConfig: {
				chart: {
					xAxis: 'name',
					height: 240,
					showLegend: true,
					showGrid: false,
					series: [{ key: 'value', label: 'Value', type: 'pie' }],
				},
			},
		},
	},
};

# Modes Overview

Mode controls the entire content area layout strategy. The same data array, the same fields, and the same actions work across all modes — only `zones.content.mode` and `modeConfig.<mode>` change.

## When to Use Each Mode

| Mode | Best for | Key characteristic |
|---|---|---|
| **table** | Structured records with many fields, sorting, grouping | Column headers; rows scan horizontally |
| **grid** | Visual card layouts; equal-weight items | Responsive tile grid; no column headers |
| **carousel** | Sequential browsing; spotlight one item at a time | Horizontal scroll; arrows and indicators |
| **board** | Status-grouped items; kanban-style workflows | Columns defined by a field value; optional drag |
| **chart** | Aggregated numeric data; trend and composition views | bar, line, area, pie, donut series types |

For automatically switching between modes at different breakpoints, see [Responsive Switching](/core/concepts/modes/responsive-switching).

## Same Data, Six Modes

The demos below use the same dataset rendered in each mode.

<div className="modes-overview-grid">
	<WidgemoShowcase className="modes-overview-preview" title="Table" data={modesOverviewData} config={modesOverviewTableConfig} />
	<WidgemoShowcase className="modes-overview-preview" title="Grid" data={modesOverviewData} config={modesOverviewGridConfig} />
	<WidgemoShowcase className="modes-overview-preview" title="Carousel" data={modesOverviewData} config={modesOverviewCarouselConfig} />
	<WidgemoShowcase className="modes-overview-preview" title="Board" data={modesOverviewData} config={modesOverviewBoardConfig} />
	<WidgemoShowcase className="modes-overview-preview" title="Bar Chart" data={modesOverviewData} config={modesOverviewBarChartConfig} />
	<WidgemoShowcase className="modes-overview-preview" title="Pie Chart" data={modesOverviewData} config={modesOverviewPieChartConfig} />
</div>

## Shared vs Mode-Specific Config

Mode guides cover settings under `modeConfig.<mode>` and mode-specific caveats.

Behavior shared across all modes lives in Reference and applies regardless of which mode is active:

- [Content Config](/core/reference/content-config) — sorting, filtering, grouping, pagination, search, gestures, responsive breakpoints, loading, and error handling
- [Action Config](/core/reference/action-config) — action shape, placement, and overflow behavior
- [Field Config](/core/reference/field-config) — item field rendering and layout contract
- [Widgemo Config](/core/reference/widgemo-config) — top-level Widgemo configuration surface

## Mode Guides

- [Table Mode](/core/concepts/modes/table)
- [Grid Mode](/core/concepts/modes/grid)
- [Carousel Mode](/core/concepts/modes/carousel)
- [Board Mode](/core/concepts/modes/board)
- [Chart Mode](/core/concepts/modes/chart)
- [Responsive Switching](/core/concepts/modes/responsive-switching)
