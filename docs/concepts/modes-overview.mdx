import { WidgemoShowcase } from '../../src/components/WidgemoShowcase';

export const modesOverviewData = [
	{ name: 'Aurora Chen', department: 'Engineering', status: 'active', value: 42 },
	{ name: 'Mateo Silva', department: 'Design', status: 'pending', value: 31 },
	{ name: 'Priya Nair', department: 'Operations', status: 'inactive', value: 27 },
	{ name: 'Samir Khan', department: 'Engineering', status: 'active', value: 50 },
];

export const modesOverviewTableConfig = {
	zones: {
		content: {
			mode: 'table',
			modeConfig: {
				table: {
					type: 'traditional',
					alternatingRows: true,
					hover: true,
					showHeader: true,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewGridConfig = {
	zones: {
		content: {
			mode: 'grid',
			modeConfig: {
				grid: {
					minItemWidth: '180px',
					maxColumns: 4,
					gap: '0.75rem',
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewCarouselConfig = {
	zones: {
		content: {
			mode: 'carousel',
			modeConfig: {
				carousel: {
					itemWidth: 220,
					itemHeight: 150,
					gap: 12,
					showArrows: true,
					showIndicators: true,
					infinite: true,
					revolving: true,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
					{ key: 'status', label: 'Status', renderAs: 'badge' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewBoardConfig = {
	zones: {
		content: {
			mode: 'board',
			modeConfig: {
				board: {
					columns: {
						field: 'status',
						items: [
							{ id: 'active', label: 'Active', value: 'active' },
							{ id: 'pending', label: 'Pending', value: 'pending' },
							{ id: 'inactive', label: 'Inactive', value: 'inactive' },
						],
					},
					dragEnabled: false,
				},
			},
			item: {
				fields: [
					{ key: 'name', label: 'Name' },
					{ key: 'department', label: 'Department' },
				],
				layout: { type: 'auto' },
			},
		},
	},
};

export const modesOverviewBarChartConfig = {
	zones: {
		content: {
			mode: 'chart',
			modeConfig: {
				chart: {
					xAxis: 'name',
					height: 240,
					showLegend: false,
					showGrid: true,
					series: [{ key: 'value', label: 'Value', type: 'bar' }],
				},
			},
		},
	},
};

export const modesOverviewPieChartConfig = {
	zones: {
		content: {
			mode: 'chart',
			modeConfig: {
				chart: {
					xAxis: 'name',
					height: 240,
					showLegend: true,
					showGrid: false,
					series: [{ key: 'value', label: 'Value', type: 'pie' }],
				},
			},
		},
	},
};

# Modes Overview

widgemo-core ships with a small set of mode guides that explain each mode-specific config surface and show how the settings change the rendered widget.

For shared mode-level behavior, start with the pages below. Each guide focuses on one configuration surface and shows the visible effect of the settings.

## Quick Look

The same dataset below is shown as a table, grid, carousel, board, and two chart types.

<div style={{ display: 'grid', gap: '1rem', gridTemplateColumns: 'repeat(2, minmax(0, 1fr))', alignItems: 'start' }}>
	<WidgemoShowcase title="Table" data={modesOverviewData} config={modesOverviewTableConfig} />
	<WidgemoShowcase title="Grid" data={modesOverviewData} config={modesOverviewGridConfig} />
	<WidgemoShowcase title="Carousel" data={modesOverviewData} config={modesOverviewCarouselConfig} />
	<WidgemoShowcase title="Board" data={modesOverviewData} config={modesOverviewBoardConfig} />
	<WidgemoShowcase title="Bar Chart" data={modesOverviewData} config={modesOverviewBarChartConfig} />
	<WidgemoShowcase title="Pie Chart" data={modesOverviewData} config={modesOverviewPieChartConfig} />
</div>

## Guides

- [Chart Mode](./modes/chart)
- [Table Mode](./modes/table)
- [Grid Mode](./modes/grid)
- [Carousel Mode](./modes/carousel)
- [Board Mode](./modes/board)
- [Responsive Switching](./modes/responsive-switching)

If you want the broader contract instead of the mode-specific settings, see [Content Config](/core/reference/content-config) and [Widgemo Config](/core/reference/widgemo-config).
